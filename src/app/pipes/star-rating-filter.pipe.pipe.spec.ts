import { StarRatingFilterPipe } from './star-rating-filter.pipe.pipe';

describe('StarRatingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StarRatingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});