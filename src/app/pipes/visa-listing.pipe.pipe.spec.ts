import { VisaListingPipe } from './visa-listing.pipe.pipe';

describe('VisaListing.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new VisaListingPipe();
    expect(pipe).toBeTruthy();
  });
});
